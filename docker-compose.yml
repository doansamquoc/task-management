version: "8.3"
services:
  db:
    container_name: db
    image: postgres:17-alpine
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql
    environment:
      POSTGRES_DB: tms
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 123456
volumes:
  db-data: